declare module "native-base" {
    namespace NativeBase {
        interface Content {
            /**
             * scrolling bounces or not.
             */
            bounces?: boolean;
        }
    }
}
